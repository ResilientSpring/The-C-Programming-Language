#include <stdio.h>

int main() {

	char msg[] = "a character array (character string)";

//	char[] an_array_of_character_strings[] = { "elide", "aggregate" };

	char an_array_of_character_strings[][] = { "elide", "aggregate" };
}