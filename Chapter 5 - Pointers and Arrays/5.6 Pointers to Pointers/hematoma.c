#include <stdio.h>

int main() {

	// char* lineptr[]; lineptr is an array of pointers to char.
	char* lines[3] = { "one", "two", "three" };

	// char** lineptr;	Declares a pointer to a pointer to char.
	char** lineptr = lines;


}