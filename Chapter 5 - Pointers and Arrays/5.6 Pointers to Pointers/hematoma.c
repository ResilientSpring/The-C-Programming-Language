#include <stdio.h>

int main() {

	// char* lines[]; lines is an array of pointers to char.
	char* lines[3] = { "one", "two", "three" };

	// char** lineptr; lineptr is a pointer to a pointer to char.
	char** lineptr = lines;


}