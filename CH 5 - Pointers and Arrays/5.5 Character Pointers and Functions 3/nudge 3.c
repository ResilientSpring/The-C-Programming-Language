#include <stdio.h>

int main() {

	// A string constant, written as below, is an array of characters.
	char* a_string_constant = "I am a string";
	// In the internal representation, this array of characters is terminated with the null 
	// character '\0' so that the compiler or OS can find the end of this array.

}