#include <stdio.h>

int main() {

	// A string constant, written as the below, is an array of characters.
	char* a_string_constant = "I am a string";
	// In the internal representation, the array is terminated with the null character '\0',
	// so that programs can find the end. 

}

